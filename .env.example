# Copy this file to ".env" and fill in your own values.
# .env is read automatically at startup (see config.py). Never commit your real .env.

# Your private ntfy.sh topic. Anyone who knows this string can read your alerts,
# so keep it secret and hard to guess. Change the default in config.py too if you like.
NTFY_TOPIC=mohith-hunt-x7k2p9

# Phase 2 only — leave blank for now.
ANTHROPIC_API_KEY=
